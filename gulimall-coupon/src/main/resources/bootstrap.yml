spring:
  application:
    name: gulimall-coupon
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yaml
        namespace: coupon
        # 开发环境
        group: dev
