spring:
  application:
    name: gulimall-third-part
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yaml
        namespace: third-part
        # 开发环境
        group: dev
