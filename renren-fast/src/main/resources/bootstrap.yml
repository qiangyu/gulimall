spring:
  application:
    name: renren-fast
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yaml
        namespace: renren-fast
        # 开发环境
        group: dev
